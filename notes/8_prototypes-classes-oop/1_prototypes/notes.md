<!-- Arrays are objects in JS and they has several methods. There are many which are prototypes (__proto__). -->
<!-- Prototypes are TEMPLATE objects, which contain a bunch of methods and we can create multiple objects that share the same prototype so they all have access to the same methods without having to have their own copy. -->
<!-- __proto__ REFERENCES the array prototype. A prototype is the template object, which contains a bunch of methods. -->
<!-- For the array example, rather than having each individual method (i.e. push) on every array there is a prototype shared amongst all, which is REFERENCED by __proto__. -->
<!-- Each type of object has a prototype object which is a part of every object of a type. You can check this in console by typing Array.prototype for example to check an array's prototype. -->
<!-- You can even create your own custom object and set it to an object's prototype such as an array's prototype. -->
